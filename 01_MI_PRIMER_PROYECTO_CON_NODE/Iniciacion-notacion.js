//Notacion
//; -> delimitar el fin de una sentencia o linea

const nombre = 'Juan';

//. ->  acceder a una propiedad de un objeto  o una variable
console.log(nombre.length);

//[] -> acceder a un elemento de un array o una propiedad de un objeto

const personas = ['Juan', 'Pedro', 'Maria'];
console.log(personas[1]);

//() -> funciones de un objeto

function  sumar(a, b) {
    return a + b;
}


//{} -> objetos, arreglos, funciones
const movile = {
    marca: 'Samsung',
    modelo: 'S10',
};


